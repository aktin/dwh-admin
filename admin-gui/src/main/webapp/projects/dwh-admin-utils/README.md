/_ "dest": "../../node_modules/@utils",_/

Usage with the package:
```
npm install ../somepath/dist/dwh-admin-utils-0.0.1.tgz
```

or with the folder, add the following path to the `tsconfig.json` - `compilerOptions` -`paths`-Array
```
"@aktin/utils": ["dist/@aktin/utils"]
```
there must be a `compilerOptions` -`baseUrl`

